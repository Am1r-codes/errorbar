[project]
name = "errorbar"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Am1r-codes", email = "amir.akkam@student.uva.nl" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
errorbar = "errorbar:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
